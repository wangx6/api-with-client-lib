/**
 * @author Yinghan Wang
 * @date
 * @licence
 */
const util = require('../util');

/**
 * Unit testing
 * @param
 * @return
 */
describe('unit', () => {
	describe('error', () => {
		test('', () => {
			expect(true).toBe(true);
		});
	});
});
